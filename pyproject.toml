[project]
name = "milvus-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pymilvus[model]>=2.6.6",
    "pytest>=9.0.2",
]
